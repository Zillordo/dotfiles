Implemented and wrote the interface design to:

`/home/allank/Documents/work/fidoo-fx-fe/context.md`

It includes:
1. Interface signature (types/methods)  
2. Usage example  
3. What the design hides internally  
4. Trade-offs  

I kept the design to **2 top-level primitives** (`CashflowSection` + `CashflowRow`) to minimize API surface while unifying category and summary semantics.