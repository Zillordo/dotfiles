# Task for worker

Design a very conservative/common-case interface for merging the same two cashflow table React components. Same requirements and callers as above. This design constraint: optimize for the most common current call sites and minimize props, even if slightly less flexible. Output: signature, usage example, hidden internals, trade-offs.