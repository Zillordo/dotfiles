# Task for worker

Design an interface for merging two React components in provider-bw/libs/bw-react-cockpit cashflow table: CashflowTableCellComponent and CashflowTableSummaryValueCellComponent. Requirements: both render a clickable MUI TableCell for a period column, shared highlight behavior via useIsColumnSelected(columnIndex), shared default click behavior (onClick override otherwise setPeriod(dayjs(dateFrom))), shared TableCell sx and colorMode logic (balance/final/none), one variant renders a simple formatted number with optional LinearProgress bar, the other renders a compact currency amount with optional CashflowQualityIndicator and accepts null values. Current callers: net balance row uses numeric value, bold, colorMode balance; category data row uses numeric value, custom onClick; final balance row uses summary value + quality indicator + currency, bold, colorMode final; income/costs categories use summary value + currency, bold, colorMode none. Constraints: React + TypeScript + MUI, preserve memoization/perf mindset, avoid over-generalizing if possible, hide highlight/click/color/styling internals, design interface only, do not implement. This design constraint: minimize method/component count, aim for 1 public component if possible. Output: a concise interface proposal with signature, example, hidden internals, trade-offs.